package com.eleap.eleap.ui.theme

import androidx.compose.ui.graphics.Color

// ── Light mode ────────────────────────────────────────────────────────────────
val Indigo40       = Color(0xFF3949AB)   // primary: xanh indigo đậm — chữ từ, nút chính
val IndigoGrey40   = Color(0xFF5C6BC0)   // secondary: indigo nhạt hơn — chip, badge
val Teal40         = Color(0xFF00796B)   // tertiary: xanh teal — nhấn phụ

// ── Dark mode ─────────────────────────────────────────────────────────────────
val Indigo80       = Color(0xFF9FA8DA)   // primary dark: indigo pastel — dễ đọc trên nền tối
val IndigoGrey80   = Color(0xFFC5CAE9)   // secondary dark
val Teal80         = Color(0xFF80CBC4)   // tertiary dark